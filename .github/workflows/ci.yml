name: Build and Publish Image To DuckerHub

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v4
    - name: build
      run: docker build . -t ramoro/convocarte-frontend-docker-hub:latest
    - name: publish
      run: docker push ramoro/convocarte-frontend-docker-hub:latest
